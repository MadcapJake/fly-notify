provide: default, clear, build

default = *->
   yield this.tasks.clear()
   yield this.tasks.build()

clear = *->
   yield this.clear("index.js")

build = *->
   yield chain this:
      @source("src/index.eg")
      @earl()
      @concat("index.js")
      @target(".")
